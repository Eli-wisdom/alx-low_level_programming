#include "main.h"

/**
 * _abs - calculates the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value in int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
